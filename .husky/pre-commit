# Run lint-staged to check and fix code style
npx lint-staged

# Note: Unit tests are skipped in pre-commit hook due to memory requirements.
# Tests run automatically in CI on push to main/feature branches.
# To run tests locally, use: NODE_OPTIONS="--max-old-space-size=8192" npm test
